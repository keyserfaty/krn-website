{
  "name": "website",
  "description": "website front end source code",
  "version": "1.0.0",
  "repository": "https://www.pixel2html.com",
  "license": "MIT",
  "scripts": {
    "start": "npm install",
    "code": "gulp",
    "build": "gulp build --prod",
    "release": "gulp release --prod",
    "lint:js": "eslint 'src/**/*.js'",
    "fix:js": "eslint 'src/**/*.js' --fix",
    "lint:scss": "stylelint 'src/**/*.scss'",
    "lint": "npm run lint:js; npm run lint:scss",
    "debug": "gulp scripts --debug"
  },
  "dependencies": {
    "@pixel2html/pipes": "^2.2.4",
    "@pixel2html/scripts-frontend": "^0.6.3",
    "del": "^3.0.0",
    "fs-path": "0.0.24",
    "gulp": "^4.0.0",
    "gulp-zip": "^4.2.0",
    "yargs": "^12.0.1"
  },
  "devDependencies": {
    "@pixel2html/stylelint-config": "^1.5.5",
    "browser-sync": "^2.26.0",
    "react-dev-utils": "^6.0.4",
    "stylelint": "^9.6.0"
  },
  "stylelint": {
    "extends": "@pixel2html/stylelint-config"
  },
  "eslintConfig": {
    "extends": "@pixel2html/eslint-config"
  }
}
